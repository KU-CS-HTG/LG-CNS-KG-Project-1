# 인터뷰 평가 회귀 분석 — 33b0480 프롬프트 변경 (2026-09-16 저녁)

## 증상
`check.py` 인터뷰 4건이 실행마다 다르게 나온다: 2/4 → 3/4 → 3/4 (다른 케이스가 떨어짐). 3질문 20건은 매번 20/20.
- I2(빈약한 자기소개·조정능력): 1위가 컴퓨터공학부 ↔ 건설환경도시공학부 사이에서 흔들림 → 직무가 DX Engineer ↔ Pack개발(ESS) 로 바뀌어 "요구 태도 ✓" 가 사라짐
- I4(마케팅): 강점 성향이 발표력·설득력 ↔ 분석성 으로 바뀜 → Communication ✓ 가 사라짐

## 실험 1 — 성향 판단(infer_orientation) 가중치는 원인이 아니다
같은 인터뷰 결과에 A(현재 1.0) / B(0.5, trait 쪽) / C(제외) 를 적용해 비교. **같은 인터뷰 출력이면 세 설정이 같은 결과**를 냈고, 흔들림은 반복(rep) 사이에서 났다.

| 케이스 | rep | 강점 성향(raw) | A 1.0 | B 0.5 | C 제외 |
|---|---|---|---|---|---|
| I2 | 1 | 조정능력·문제해결 | ✓ 컴공 | ✗ 건설환경 | ✓ 컴공 |
| I2 | 2 | 조정능력·문제해결 (동일) | ✗ 건설환경 | ✗ | ✗ |
| I4 | 1 | 발표력·설득력 | ✓ | ✓ | ✓ |
| I4 | 2 | **분석성만** | ✗ | ✗ | ✗ |
| I1·I3 | 1 | — | ✓ | ✓ | ✓ |

→ 흔들리는 곳은 두 LLM 호출: ① 강점 분석(I4: 같은 입력에 다른 성향) ② 태거(I2: 같은 텍스트에 Software Engineering 이 붙었다 안 붙었다). 가중치로는 못 잡는다.

## 실험 2 — 프롬프트를 33b0480 이전(8c457a1)으로 되돌리면 안정된다
`user_analysis/prompts.py` 만 임시로 이전 버전으로 바꾸고 같은 케이스를 2회씩:

| 케이스 | rep1 | rep2 |
|---|---|---|
| I2 | ✓ 컴공 · 관심 Data Analysis + **Software Engineering** · 성향판단 Software Engineering · 태도 ✓ | ✓ 동일 |
| I4 | ✓ 경영학과 · 강점 **발표력·설득력** · 태도 Communication ✓ | ✓ 동일 |

→ **4/4, 두 번 모두 같은 출력.** 33b0480 은 약점 영역을 빼면서 프롬프트의 다른 규칙("STANDARD_TRAITS 중 최대 3개", "confidence 0~1 평가", "판단 근거 evidence 작성" 등)도 함께 지웠고(−101줄), 그 결과 성향 추출과 관심 정리가 실행마다 달라진다.

## 제안
- `user_analysis/prompts.py` 를 **8c457a1 버전으로 되돌리고, 약점(weakness) 관련 줄만 제거**한다. 스키마·profile_config·vocabulary 의 약점 삭제는 그대로.
- 되돌린 뒤 `python check.py` 두 번 연속 4/4 인지 확인.
- 성향 판단 가중치(1.0 vs 0.5)는 별개 논의 — 이번 회귀와 무관.
